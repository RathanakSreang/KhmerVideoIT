<div class="row">
  <div class="col-md-12">
    <h3><%= link_to article.title, article %></h3>
    <p>
      upload at: <%= article.created_at.to_date %>, <%= article.comments.size %> comments</p>
    <p>
    <p class="acticle-info">Tags:
    <% article.tags.each do |tag| %>
      <%= link_to tag.name, tag %>
    <% end %>
    </p>    
      <%= article.description %>
      <%= link_to "read more.", article_path(article) %> 
    </p>
  </div>
</div>
