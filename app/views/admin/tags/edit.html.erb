<div class="row">
  <div class="col-md-4 col-md-offset-4 well">
    <%= link_to :back do %>
      <i class="fa fa-arrow-left"></i> <%= t("link.back") %>
    <% end %>
    <legend class="center"><%= t("admin.tags.label.edit_tag") %></legend>
    <%= render "form", action: t("action.update") %>
  </div>
</div>
