<% if question %>
  <%= t("admin.activities.question.add_question") %> <%= link_to question.title, [:admin, question] %>  
<% else %>  
  <%= t("admin.activities.question.add_remove_question") %>
<% end %>
