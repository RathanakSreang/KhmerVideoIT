<% if I18n.locale == :en %>
  <p>Welcome <%= @resource.name %>!</p>

  <p>Thanks for signing up for Let Talk IT! Please click the link below to confirm your email address.</p>

  <p><%= link_to confirmation_url(@resource, confirmation_token: @token) , confirmation_url(@resource, confirmation_token: @token) %></p>
<% else %>
  <p>Welcome <%= @resource.name %>!</p>

  <p>Thanks for signing up for Let Talk IT! Please click the link below to confirm your email address.</p>

  <p><%= link_to confirmation_url(@resource, confirmation_token: @token) , confirmation_url(@resource, confirmation_token: @token) %></p>
<% end %>
