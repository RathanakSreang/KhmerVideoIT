<div class="row">  
  <div class="col-md-12">
    <legend><%= t("static_page.label.recent_video") %></legend>
    <%= render @videos %>
  </div>
  <div class="col-md-12">
    <legend><%= t("static_page.label.recent_article") %></legend>
    <%= render @articles %>
  </div>
</div>
